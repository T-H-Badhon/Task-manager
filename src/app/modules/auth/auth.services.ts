

const userRegister = async (payload)=>{
    console.log(payload)

    return payload
}

const userLogin = async (payload)=>{
    console.log(payload)

    return payload
}

export const authServices = {
    userLogin,
    userRegister
}